<Window x:Class="TPALCommander.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPALCommander"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File"></MenuItem>
        </Menu>
        <Menu DockPanel.Dock="Top" Height="24">
            <MenuItem Width="24">
                <MenuItem.Icon>
                    <Image Source="Assets/icon-refresh-128.png"
                           RenderOptions.BitmapScalingMode="Fant"/>
                </MenuItem.Icon>
            </MenuItem>
        </Menu>
        <Grid Background="AliceBlue" x:Name="MainGrid" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Column="0" x:Name="LeftGrid"/>
            <Label Content="{Binding Dir}"></Label>
            <ListView Grid.Column="0" Grid.Row="1"  HorizontalAlignment="Stretch" 
                      Margin="1,0,2,1" Name="LeftView" Background="Aqua" 
                      SizeChanged="List_SizeChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <EventSetter Event="MouseDoubleClick" Handler="listViewItem_MouseDoubleClick"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" 
                                        DisplayMemberBinding="{Binding Name}"
                                        Width="Auto" />
                        <GridViewColumn Header="Extention" 
                                        DisplayMemberBinding="{Binding Extension}"
                                        Width="Auto" />
                        <GridViewColumn Header="Weight" 
                                        DisplayMemberBinding="{Binding Size}"
                                        Width="Auto"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <ListView Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" 
                      Margin="2,0,1,1" Name="RightView" Background="Red">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseDoubleClick" Handler="listViewItem_MouseDoubleClick"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" 
                                        DisplayMemberBinding="{Binding Name}"
                                        Width="Auto" 
                                        />
                        <GridViewColumn Header="Extention" 
                                        DisplayMemberBinding="{Binding Extension}"
                                        Width="Auto" />
                        <GridViewColumn Header="Weight" 
                                        DisplayMemberBinding="{Binding Weight}"
                                        Width="Auto"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>

        


        
